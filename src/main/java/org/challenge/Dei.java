package org.challenge;

public class Dei {

    public static void main(String[] args) {
        /*Mutable s1=0;
        List<String> list = List.of("5 de diamantes", "3 de picas", "1 de corazon", "as de corazones", "4 de treboles", "3 de treboles");
        //"3 de treboles","as de corazones", 3 de picas", "5 de diamantes", "1 de corazon","4 de treboles"
        LinkedList<String> link = new LinkedList<>();
        list.forEach(c -> {
            int index = list.indexOf(c);
            //Integer a
            if (index == 0) {
                link.add(c);
            } else if (index % 2 != 0) {
                link.addLast(c);
            } else {
                link.addFirst(c);
            }
        });


        list.stream().filter(s->s.equals(s1)).toList();
        System.out.println("link = " + link);*/
        System.out.println("Hello, World!");
    }
}
